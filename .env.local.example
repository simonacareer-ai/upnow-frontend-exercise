# Requests go through the Next.js rewrite proxy, not straight to staging —
# calling the API directly from the browser is blocked by CORS.
# Set the rewrite up first: see section 1 of API.md.
NEXT_PUBLIC_API_URL=/api
